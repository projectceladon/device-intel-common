# this is default configuration for bluetooth controller on intel products
# UART device port where Bluetooth controller is attached
UartPort = /dev/ttyHSU0

# Firmware patch file location
FwPatchFilePath = /system/vendor/etc/firmware/bt/
